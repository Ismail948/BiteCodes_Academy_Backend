FROM eclipse-temurin:17-jdk-alpine

WORKDIR /app

# Wildcard will work only if there's a single JAR in target/
COPY target/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "/app/app.jar"]
